# Frontend (Vite) — injetadas no BUILD, visíveis no navegador.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your-anon-key

# ----------------------------------------------------------
# A key da OpenAI NÃO fica aqui. Ela vive só no servidor,
# como secret da Edge Function (veja supabase/.env.example):
#   supabase secrets set OPENAI_API_KEY=sk-... OPENAI_MODEL=gpt-4o-mini
# ----------------------------------------------------------
