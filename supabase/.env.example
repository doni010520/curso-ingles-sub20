# Secrets da Edge Function 'quiz' (lado servidor — nunca vão pro front).
# Local:  copie para supabase/.env e rode  supabase functions serve --env-file supabase/.env
# Deploy: supabase secrets set OPENAI_API_KEY=sk-... OPENAI_MODEL=gpt-4o-mini
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
